<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.TelefoneActivity">

    <android.support.v7.widget.RecyclerView
        android:layout_marginTop="100dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recyclerContatos"></android.support.v7.widget.RecyclerView>


    <ProgressBar
        android:id="@+id/progressContatos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:layout_centerHorizontal="true"
        android:visibility="invisible" />
    <Button
        android:id="@+id/btnBuscarCidades"
        android:background="@color/colorPrimary"
        android:textColor="#ffff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="54dp"
        android:layout_marginRight="54dp"
        android:layout_marginTop="23dp"
        android:text="Buscar" />

    <Spinner
        android:id="@+id/spnCidades"
        android:layout_width="200dp"
        android:layout_height="45dp"
        android:layout_alignBottom="@+id/btnBuscarCidades"
        android:layout_toLeftOf="@+id/btnBuscarCidades"
        android:layout_toStartOf="@+id/btnBuscarCidades" />

</RelativeLayout>